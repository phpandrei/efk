<source>
  @type forward
  port 24224
</source>

<source>
  @type tail
#  path /var/www/app/path/to/*.log
  path /data/my/*.log
  tag test
  <parse>
    @type json
  </parse>
</source>

#<source>
#  @type tail
##  path /var/www/app/path/to/*.log
#  path /data/my/*.log
#  tag testregexp
#  <parse>
#    @type regexp
#    expression ^(?<mytime>.*)\s(?<type>\[\w+\])\s(?<pid>\d+#\d+):\s(?<log>[^ ].*)
#  </parse>
#</source>

<match *.**>
  @type copy
  <store>
    @type elasticsearch
    host elasticsearch
    port 9200
    logstash_format true
    logstash_prefix fluentd
    logstash_dateformat %Y%m%d
    include_tag_key true
    type_name access_log
    tag_key @log_name
    flush_interval 1s
  </store>
</match>

<match test>
  @type copy
  <store>
    @type elasticsearch
    host elasticsearch
    port 9200
    logstash_format true
    logstash_prefix fluentd
    logstash_dateformat %Y%m%d
    include_tag_key true
    type_name access_log
    tag_key @log_name
    flush_interval 1s
  </store>
</match>

#<match testregexp>
#  @type copy
#  <store>
#    @type elasticsearch
#    host elasticsearch
#    port 9200
#    logstash_format true
#    logstash_prefix fluentd
#    logstash_dateformat %Y%m%d
#    include_tag_key true
#    type_name access_log
#    tag_key @log_name
#    flush_interval 1s
#  </store>
#</match>
#
#<match test>
#    @type copy
#    <store>
#       @type file
#       path /data/log/testBackup.log
#    </store>
#</match>